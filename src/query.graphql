query GetCompany {
    getCompany {
        data {
            id
            name
        }
    }
}
query GetLaunchList {
    launches {
        flight_number
        mission_name
        launch_year
    }
}